<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog.benggri.springboot.member.MemberDao">

    <select id="getDataList" parameterType="map" resultType="map">
        select /* MemberDao.getDataList */
               seq    as seq    /* numeric-순번 */
             , id     as id     /* varchar-ID */
             , pwd    as pwd    /* varchar-비밀번호 */
             , auth   as auth   /* varchar-인증 */
             , reg_dt as reg_dt /* timestamp-등록_일시 */
             , chg_dt as chg_dt /* timestamp-수정_일시 */
          from member /* 사용자 */
         where 1=1
        <if test=' id != "" and id != null '>
           and id = #{id} /* varchar-ID */
        </if>
    </select>

    <select id="getData" parameterType="map" resultType="map">
        select /* MemberDao.getData */
               seq    as seq    /* numeric-순번 */
             , id     as id     /* varchar-ID */
             , pwd    as pwd    /* varchar-비밀번호 */
             , auth   as auth   /* varchar-인증 */
             , reg_dt as reg_dt /* timestamp-등록_일시 */
             , chg_dt as chg_dt /* timestamp-수정_일시 */
          from member /* 사용자 */
         where 1=1
           and seq = cast(#{seq} as numeric) /* numeric-순번 */
    </select>

</mapper>
